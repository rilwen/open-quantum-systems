Import('env')
Import('qsd')
Import('protein_chain')
Import('math')
Import('GTEST_LIBS')
Import('OTHER_LIBS')
penv = env.Clone()
penv.Append(LIBS = [qsd, protein_chain, math] + GTEST_LIBS + OTHER_LIBS)
penv.Append(CPPPATH = '#protein_chain')
qsd_test = penv.Program('qsd-test', Glob('*.cpp'))
Return('qsd_test')